[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests standalone_discovery/tests
markers =
    integration: тесты с живой PostgreSQL (QUEUE_DATABASE_URL)
    tz30: сценарии приёмки §30 ТЗ (tz-extract.txt)
    e2e: сквозные сценарии через enqueue + worker + PG
